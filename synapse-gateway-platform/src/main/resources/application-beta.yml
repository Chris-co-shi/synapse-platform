spring:
  cloud:
    nacos:
      discovery:
        namespace: ${NACOS_NAMESPACE:beta}
        group: ${NACOS_GROUP:SYNAPSE_PLATFORM_BETA}
      config:
        namespace: ${NACOS_NAMESPACE:beta}
        group: ${NACOS_GROUP:SYNAPSE_PLATFORM_BETA}

logging:
  level:
    com.indigo.synapse: INFO

synapse:
  gateway:
    proof:
      enabled: ${GATEWAY_PROOF_ENABLED:true}
