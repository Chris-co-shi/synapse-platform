name: synapse-gateway

services:
  gateway:
    image: "${GATEWAY_IMAGE_REPOSITORY:?GATEWAY_IMAGE_REPOSITORY is required}:${GATEWAY_IMAGE_TAG:?GATEWAY_IMAGE_TAG is required}"
    pull_policy: "${GATEWAY_PULL_POLICY:-always}"
    restart: unless-stopped
    ports:
      - "${GATEWAY_HOST_PORT:-20000}:${SERVER_PORT:-20000}"
    environment:
      SPRING_PROFILES_ACTIVE: "${SPRING_PROFILES_ACTIVE:-prd}"
      SERVER_PORT: "${SERVER_PORT:-20000}"
      NACOS_SERVER_ADDR: "${NACOS_SERVER_ADDR:?NACOS_SERVER_ADDR is required}"
      NACOS_NAMESPACE: "${NACOS_NAMESPACE:-prd}"
      NACOS_GROUP: "${NACOS_GROUP:-SYNAPSE_PLATFORM_PRD}"
      NACOS_USERNAME: "${NACOS_USERNAME:-}"
      NACOS_PASSWORD: "${NACOS_PASSWORD:-}"
      IAM_ISSUER_URI: "${IAM_ISSUER_URI:?IAM_ISSUER_URI is required}"
      IAM_JWK_SET_URI: "${IAM_JWK_SET_URI:?IAM_JWK_SET_URI is required}"
      SYNAPSE_GATEWAY_AUDIENCE: "${SYNAPSE_GATEWAY_AUDIENCE:-synapse-platform}"
      GATEWAY_PROOF_ENABLED: "${GATEWAY_PROOF_ENABLED:-true}"
      GATEWAY_ID: "${GATEWAY_ID:-synapse-gateway}"
      GATEWAY_PROOF_SECRET: "${GATEWAY_PROOF_SECRET:-}"
      JAVA_TOOL_OPTIONS: "${JAVA_TOOL_OPTIONS:-}"
      TZ: "${TZ:-UTC}"
      # 仅用于无外部依赖的本地容器演练；生产默认保持 Nacos Discovery/Config 开启。
      SPRING_CLOUD_NACOS_DISCOVERY_ENABLED: "${SPRING_CLOUD_NACOS_DISCOVERY_ENABLED:-true}"
      SPRING_CLOUD_NACOS_CONFIG_ENABLED: "${SPRING_CLOUD_NACOS_CONFIG_ENABLED:-true}"
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          curl --fail --silent --show-error --max-time 4
          "http://127.0.0.1:$${SERVER_PORT}/actuator/health/readiness" >/dev/null
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 40s
    stop_grace_period: 40s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp:rw,nosuid,nodev,size=64m
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "5"
    networks:
      - synapse-platform

networks:
  synapse-platform:
    name: "${SYNAPSE_DOCKER_NETWORK:-synapse-platform}"
    driver: bridge
